#!/usr/bin/env zsh

# Taps
brew tap homebrew/bundle
brew tap homebrew/cask
brew tap homebrew/cask-fonts
brew tap homebrew/core
brew tap homebrew/services
brew tap symfony-cli/tap

# PHP utilities
brew install composer symfony-cli

# Fonts
brew install font-andale-mono font-dejavu font-fira-code-nerd-font font-inconsolata font-sauce-code-pro-nerd-font font-source-code-pro

# Misc. programs
brew install sublercli
